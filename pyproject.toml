[project]
name = "chargeback"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "anthropic[bedrock]",
    "asyncpg",
    "torch>=2.13.0",
    "numpy>=2.5.2",
    "scikit-learn>=1.9.0",
    "onnx>=1.22.0",
    "onnxruntime>=1.29.0",
]

[dependency-groups]
dev = [
    "pytest",
    "pytest-asyncio",
    "httpx",
    "ruff"
]

[tool.pytest.ini_options]
pythonpath = ["."]
